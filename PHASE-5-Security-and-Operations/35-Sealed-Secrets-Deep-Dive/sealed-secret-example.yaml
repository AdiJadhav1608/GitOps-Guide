# Sealed Secret Deep Dive Example
# Fixed version (no duplicate metadata)

apiVersion: bitnami.com/v1alpha1
kind: SealedSecret

metadata:
  name: database-secret
  namespace: default

spec:

  encryptedData:
    username: AgByYW5kb21fdXNlcm5hbWU=
    password: AgByYW5kb21fcGFzc3dvcmQ=

  template:
    metadata:
      name: database-secret
      namespace: default

      # Labels added correctly inside metadata
      labels:
        app: my-app

    type: Opaque