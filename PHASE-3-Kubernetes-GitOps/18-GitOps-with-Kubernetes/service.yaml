# Kubernetes Service
# Service exposes pods to internal or external network

apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: gitops-demo

spec:
  selector:
    app: nginx

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80

  type: ClusterIP   # Exposes service inside the cluster