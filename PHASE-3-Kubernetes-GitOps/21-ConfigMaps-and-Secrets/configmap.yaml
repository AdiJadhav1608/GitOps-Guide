# Kubernetes ConfigMap Example
# Used to store non-sensitive configuration values

apiVersion: v1
kind: ConfigMap

metadata:
  name: app-config
  namespace: gitops-demo

data:
  APP_ENV: "production"
  APP_VERSION: "1.0"
  LOG_LEVEL: "info"
  API_ENDPOINT: "https://api.example.com"