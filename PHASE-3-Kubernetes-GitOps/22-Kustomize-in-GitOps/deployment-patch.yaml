# Patch file used to modify an existing deployment
# This example increases replica count and changes container image

apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-deployment

spec:
  replicas: 3

  template:
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
