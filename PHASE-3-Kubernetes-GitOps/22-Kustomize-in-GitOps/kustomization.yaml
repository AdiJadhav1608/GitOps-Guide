# Kustomize configuration file
# This file tells Kubernetes how to customize the base resources

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml

patchesStrategicMerge:
  - deployment-patch.yaml

commonLabels:
  managed-by: gitops